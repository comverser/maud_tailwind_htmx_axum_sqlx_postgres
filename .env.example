DATABASE_URL=postgresql://root:postgres@localhost:5432/mthasp
SERVER_ADDR=127.0.0.1:8000

# Email Configuration
# Mode: "console" (logs to console, for development) or "smtp" (sends real emails)
EMAIL_MODE=console
BASE_URL=http://127.0.0.1:8000

# Email settings (used in emails)
EMAIL_FROM_ADDRESS=noreply@example.com
EMAIL_FROM_NAME=Your App Name

# SMTP settings (only required if EMAIL_MODE=smtp)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
